Build dependencies
==================

* Optional dependencies
  * LIBRT (required if --enable-multiprocess is given)
  * EXPAT (required if --enable-components is given)

* Required dependencies
  * XERCES-C
  * CURL
  * SDL2
  * SDL2_image
  * SDL2_ttf
  * FFMPEG (compiled with: --enable-shared --enable-static --build-suffix=ginga --disable-programs --enable-gpl --prefix=/usr --incdir=/usr/include/ffmpegginga)
    + libavcodec
    + libavdevice
    + libavfilter
    + libavformat
    + libavutil
    + libswresample
    + libswscale
    + libpostproc
  * NCLUA
  * Berkelium

Build tutorial Ubuntu 14.04 build guide
========================

* Installing depencies

  sudo apt-get install -y libcurl4-gnutls-dev libexpat1-dev libxerces-c28 libsdl2-ttf-dev libsdl2-image-dev  libsdl2-dev liblua5.1-dev
  sudo echo "deb http://www.telemidia.puc-rio.br/~rafaeldiniz/ginga4linux/ubuntu amd64/ " >> /etc/apt/sources.list
  sudo apt-get update
  sudo apt-get install -y berkelium ffmpegext  nclua

* Apply Ubuntu patch to use Rafael Diniz's ffmpeg insted of Ubuntu libav.

  ./debian/patches/fix_configure_ac_ffmpeg.sh

* Build ginga

  ./bootstrap
  ./configure --enable-data-processing --enable-video4linux --libdir=/usr/lib --prefix=/usr/ LDFLAGS="-Wl,--no-as-needed" CPPFLAGS="-I/usr/include/mirclient -I/usr/include/mircommon/" CFLAGS="-I/usr/include/mirclient -I/usr/include/mircommon/"
  make -j4
  sudo make -j4 install
